name: CI

on:
  push:
    branches: [ Hemanthi_Deepak_Kannan ]
  pull_request:
    branches: [ Hemanthi_Deepak_Kannan ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-python@v4
        with:
          python-version: "3.10"
      - run: pip install -r backend/requirements.txt
      - run: echo "Lint step placeholder"
